diff --git a/dist/src/runtime/emit/constraint-check.js b/dist/src/runtime/emit/constraint-check.js
index ef1d270b44c4cb7bed31d168c794f21d1ea88f7e..3ea1f6a0ffe7365f3b0917c540da84a2e95e01d2 100644
--- a/dist/src/runtime/emit/constraint-check.js
+++ b/dist/src/runtime/emit/constraint-check.js
@@ -268,7 +268,18 @@ async function checkCheckConstraints(rctx, plan, tableSchema, constraintMetadata
         const mustEvaluateNow = effectiveAction !== ConflictResolution.ABORT;
         if (metadata.shouldDefer && !mustEvaluateNow) {
             const activeConnectionId = rctx.activeConnection?.connectionId;
-            rctx.db._queueDeferredConstraintRow(metadata.baseTable, metadata.constraintName, flatRow, metadata.flatRowDescriptor, evaluator, activeConnectionId, metadata.contextRow, metadata.contextDescriptor);
+            // VoteTorrent patch (datetime-immediate-check-coercion), part 2 of 2:
+            // restore 4.3.1's `coerceNewSection` on the DEFERRED path. 4.3.1 kept a
+            // deliberate split — IMMEDIATE CHECKs saw the value as provided, DEFERRED
+            // CHECKs saw the coerced form, because a deferred subquery compares NEW
+            // against already-coerced stored rows in OTHER tables (coerced-vs-coerced
+            // equality). 4.4.1 collapsed the split to coerced-everywhere by converting
+            // up front in emitInsert/emitUpdate and deleted coerceNewSection. Since
+            // this patch turns the up-front conversion back off (see insert.js), the
+            // deferred snapshot has to coerce again or cross-table digest equality
+            // (e.g. Registrant.MutationValid comparing Digest(..., new.Expiration, ...)
+            // against a stored AdminSigning.Digest) breaks.
+            rctx.db._queueDeferredConstraintRow(metadata.baseTable, metadata.constraintName, coerceNewSection(flatRow, tableSchema), metadata.flatRowDescriptor, evaluator, activeConnectionId, metadata.contextRow, metadata.contextDescriptor);
             continue;
         }
         // Resolve without a per-row microtask hop (see runtime/async-util.ts).
@@ -296,6 +307,33 @@ async function checkCheckConstraints(rctx, plan, tableSchema, constraintMetadata
     }
     return { skip: false };
 }
+/**
+ * VoteTorrent patch (datetime-immediate-check-coercion), part 2 of 2.
+ * Verbatim port of 4.3.1's `coerceNewSection` (deleted in 4.4.1). Snapshot the flat
+ * OLD/NEW row for deferred evaluation, coercing the NEW half to declared column types.
+ * OLD values (0..n-1) are NULL on INSERT or read from already-coerced stored rows on
+ * UPDATE, so they are left untouched. A per-cell parse failure falls back to the raw
+ * value, preserving the existing error semantics — the row's own performInsert remains
+ * the authoritative place that throws MISMATCH.
+ */
+function coerceNewSection(row, tableSchema) {
+    const numCols = tableSchema.columns.length;
+    const snapshot = row.slice();
+    for (let i = 0; i < numCols; i++) {
+        const newIndex = numCols + i;
+        if (newIndex >= snapshot.length)
+            break;
+        const column = tableSchema.columns[i];
+        const value = snapshot[newIndex];
+        try {
+            snapshot[newIndex] = validateAndParse(value, column.logicalType, column.name);
+        }
+        catch {
+            // Keep the raw value; downstream performInsert reports the error as today.
+        }
+    }
+    return snapshot;
+}
 function generateDefaultConstraintName(tableSchema, constraint) {
     // Find the index of this constraint in the original array to get the correct constraint number
     const originalIndex = tableSchema.checkConstraints.findIndex((c) => c === constraint);
diff --git a/dist/src/runtime/emit/dml-executor.js b/dist/src/runtime/emit/dml-executor.js
index 61980aeb20358a2e7ba2dda7d6e7fad8e7e47db2..f22b006ce4d0fe116f9a1347410cbad7f90aa9fd 100644
--- a/dist/src/runtime/emit/dml-executor.js
+++ b/dist/src/runtime/emit/dml-executor.js
@@ -389,14 +389,16 @@ export function emitDmlExecutor(plan, ctx) {
         }
         // Extract the primary key from existing row
         const keyValues = pkColumnIndicesInSchema.map(idx => existingRow[idx]);
-        // Perform the UPDATE operation. `preCoerced`: the row is composed of
-        // stored cells plus assignments already converted above.
+        // Perform the UPDATE operation.
+        // VoteTorrent patch (datetime-immediate-check-coercion): `preCoerced` forced
+        // to false — update.js no longer converts the NEW row up front, so the storage
+        // layer must coerce here as it did on 4.3.1.
         const updateArgs = {
             operation: 'update',
             values: updatedRow,
             oldKeyValues: keyValues,
             onConflict: ConflictResolution.ABORT,
-            preCoerced: true,
+            preCoerced: false,
             mutationStatement: vtab.wantStatements ?
                 buildUpdateStatement(tableSchema, updatedRow, keyValues, contextRow) : undefined
         };
@@ -665,10 +667,11 @@ export function emitDmlExecutor(plan, ctx) {
             // can fall back to per-constraint defaultConflict directives. The memory
             // module treats undefined as ABORT when no constraint default is set.
             onConflict: plan.onConflict,
-            // The emitters converted the NEW section to declared types up front
-            // (emitInsert / constraint-check DEFAULT substitution), so the storage
-            // layer must not convert again — JSON conversion is not repeatable.
-            preCoerced: true,
+            // VoteTorrent patch (datetime-immediate-check-coercion): `preCoerced`
+            // forced to false — emitInsert no longer converts the NEW section up
+            // front (so immediate CHECKs see the value as provided), so the storage
+            // layer must coerce here as it did on 4.3.1.
+            preCoerced: false,
             mutationStatement
         };
         const result = await vtab.update(args);
@@ -862,10 +865,10 @@ export function emitDmlExecutor(plan, ctx) {
             // can fall back to per-constraint defaultConflict directives. The memory
             // module treats undefined as ABORT when no constraint default is set.
             onConflict: plan.onConflict,
-            // emitUpdate converted the NEW section to declared types (assigned
-            // columns by static type, unassigned columns are stored values) — the
-            // storage layer must not convert again.
-            preCoerced: true,
+            // VoteTorrent patch (datetime-immediate-check-coercion): `preCoerced`
+            // forced to false — emitUpdate no longer converts the NEW section up
+            // front, so the storage layer must coerce here as it did on 4.3.1.
+            preCoerced: false,
             mutationStatement
         };
         const result = await vtab.update(args);
diff --git a/dist/src/runtime/emit/insert.js b/dist/src/runtime/emit/insert.js
index 9402d71b8efbf72e2dfbe1a6e4430adea271bd5a..12ea2930dd1365155731f70b871a1b2a4a26b8d8 100644
--- a/dist/src/runtime/emit/insert.js
+++ b/dist/src/runtime/emit/insert.js
@@ -14,8 +14,23 @@ export function emitInsert(plan, ctx) {
     // positionally with table columns. A cell whose source type already IS the
     // column's type (e.g. `insert into b select j from a` for a JSON column) is
     // left alone — see buildRowCoercion.
-    const sourceAttrs = plan.source.getAttributes();
-    const coerceNewRow = buildRowCoercion(tableSchema.columns.map((_, i) => sourceAttrs[i]?.type.logicalType), tableSchema.columns);
+    // ---------------------------------------------------------------------------
+    // VoteTorrent patch (datetime-immediate-check-coercion): DISABLED.
+    //
+    // 4.4.1 moved declared-type conversion from the storage layer up to the TOP of
+    // the DML pipeline (this call), so an IMMEDIATE CHECK now evaluates the coerced
+    // storage form instead of the value as provided. For a `datetime` column the
+    // coercion drops the trailing `Z`, so a shape check such as
+    //   constraint ExpirationValid check (isISODatetime(Expiration) and like('%Z', Expiration))
+    // fails on input that passed identically on 4.3.1.
+    //
+    // Restore 4.3.1 semantics: leave the NEW row RAW here so immediate CHECKs see
+    // the value as provided, and let the storage layer coerce as it did in 4.3.1
+    // (see the matching `preCoerced: false` restorations in dml-executor.js).
+    // Upstream issue: gotchoices/quereus — "datetime Z-stripped before immediate CHECK".
+    // Remove this patch once upstream settles raw-vs-coerced for immediate CHECKs.
+    // ---------------------------------------------------------------------------
+    const coerceNewRow = undefined;
     async function* run(_ctx, sourceValue) {
         for await (const sourceRow of sourceValue) {
             // Convert source row to flat OLD/NEW format
diff --git a/dist/src/runtime/emit/schema-declarative.js b/dist/src/runtime/emit/schema-declarative.js
index dd17c058f1b21ae25f7f79a918da0064a8e6db17..36583d955743f3eb7f6b60b2ef6e80b7bb518aa6 100644
--- a/dist/src/runtime/emit/schema-declarative.js
+++ b/dist/src/runtime/emit/schema-declarative.js
@@ -308,6 +308,19 @@ export function emitExplainSchema(plan, _ctx) {
  * pay nothing — they're filtered out before the loop.
  */
 async function runBatchedMigrationLoop(db, schemaName, migrationStatements) {
+    // VoteTorrent patch: when applying a non-main schema (e.g. 'App'), temporarily
+    // prepend the target schema to the schema_path so that CREATE VIEW body validation
+    // can resolve unqualified table references (e.g. `from Admin`) against the target
+    // schema where the tables were just created. 4.x eagerly validates the view body
+    // at CREATE VIEW plan time (unlike 3.3.0 which deferred validation); without this
+    // the body references fail with "Table 'X' not found in schema path: App".
+    // We restore the original schema_path after the migration loop.
+    const originalPath = db.getSchemaPath ? db.getSchemaPath() : null;
+    const needsPathUpdate = schemaName && schemaName.toLowerCase() !== 'main' && schemaName.toLowerCase() !== 'temp';
+    if (needsPathUpdate && db.setSchemaPath && originalPath) {
+        const pathWithTarget = [schemaName, ...originalPath.filter(s => s.toLowerCase() !== schemaName.toLowerCase())];
+        db.setSchemaPath(pathWithTarget);
+    }
     const startedModules = await beginSchemaBatchAll(db, schemaName);
     let loopError;
     try {
@@ -329,6 +342,10 @@ async function runBatchedMigrationLoop(db, schemaName, migrationStatements) {
     }
     finally {
         await endSchemaBatchAll(startedModules, db, schemaName, loopError);
+        // Restore original schema_path after migration (undo the temporary prepend above).
+        if (needsPathUpdate && db.setSchemaPath && originalPath) {
+            db.setSchemaPath(originalPath);
+        }
     }
 }
 /**
diff --git a/dist/src/runtime/emit/update.js b/dist/src/runtime/emit/update.js
index dd9a3f377d0c6bf2a3eacc4d0f374581547f867c..2187f841c411678fd0215b2852781f5b7445a77b 100644
--- a/dist/src/runtime/emit/update.js
+++ b/dist/src/runtime/emit/update.js
@@ -48,21 +48,16 @@ export function emitUpdate(plan, ctx) {
     // type differs), phase 2 the generated cells. A generated column left in
     // place by phase 1 still holds its scanned value, which is already in
     // declared form.
-    const sourceAttrs = plan.source.getAttributes();
-    const regularCellTypes = tableSchema.columns.map((_, i) => sourceAttrs[i]?.type.logicalType);
-    for (const i of regularIndices) {
-        regularCellTypes[assignmentTargetIndices[i]] = plan.assignments[i].value.getType().logicalType;
-    }
-    const coerceRegular = buildRowCoercion(regularCellTypes, tableSchema.columns);
-    // After phase 1 every non-generated cell is in declared form (identity ⇒
-    // skip), so only the generated cells can still need converting.
-    const generatedCellTypes = tableSchema.columns.map(column => column.logicalType);
-    for (const i of generatedIndices) {
-        generatedCellTypes[assignmentTargetIndices[i]] = plan.assignments[i].value.getType().logicalType;
-    }
-    const coerceGenerated = generatedIndices.length > 0
-        ? buildRowCoercion(generatedCellTypes, tableSchema.columns)
-        : undefined;
+    // ---------------------------------------------------------------------------
+    // VoteTorrent patch (datetime-immediate-check-coercion): DISABLED.
+    // Mirror of the insert.js restoration — see the note there. The UPDATE path
+    // coerces the NEW row in two phases (regular assignments, then generated
+    // columns); both are disabled so an immediate CHECK on UPDATE evaluates the
+    // assigned value as provided, exactly as on 4.3.1. Storage coerces instead
+    // (`preCoerced: false` in dml-executor.js).
+    // ---------------------------------------------------------------------------
+    const coerceRegular = undefined;
+    const coerceGenerated = undefined;
     async function* run(rctx, sourceRowsIterable, ...assignmentEvaluators) {
         const slot = createRowSlot(rctx, sourceRowDescriptor);
         try {
diff --git a/dist/src/schema/schema-differ.js b/dist/src/schema/schema-differ.js
index 91ba9419d2c4a3a30ae260ebacc3a30ad31d67d2..9d8df7c159a8295c77b63763253ae754d2815a93 100644
--- a/dist/src/schema/schema-differ.js
+++ b/dist/src/schema/schema-differ.js
@@ -379,7 +379,7 @@ defaultCollation = 'BINARY') {
     for (const [name, declaredView] of declaredViews) {
         const matchedActual = viewRenames.pairs.get(name);
         if (!matchedActual) {
-            diff.viewsToCreate.push(createViewToString(declaredView.viewStmt));
+            diff.viewsToCreate.push(createViewToString(applyViewDefaults(declaredView.viewStmt, targetSchemaName)));
             continue;
         }
         const stmt = declaredView.viewStmt;
@@ -391,7 +391,7 @@ defaultCollation = 'BINARY') {
         }
         if (definitionDrifted) {
             diff.viewsToDrop.push(matchedActual.name);
-            diff.viewsToCreate.push(createViewToString(stmt));
+            diff.viewsToCreate.push(createViewToString(applyViewDefaults(stmt, targetSchemaName)));
             viewRecreates++;
             continue;
         }
@@ -399,7 +399,7 @@ defaultCollation = 'BINARY') {
             // Hinted rename, definition unchanged → drop(old) + recreate(declared),
             // rendered with in-diff column renames inverse-applied (NEW→OLD).
             diff.viewsToDrop.push(matchedActual.name);
-            diff.viewsToCreate.push(createViewToString(columnReconciledViewStmt(stmt, columnRenamesByTable, targetSchemaName, resolveDeclaredColumn)));
+            diff.viewsToCreate.push(createViewToString(applyViewDefaults(columnReconciledViewStmt(stmt, columnRenamesByTable, targetSchemaName, resolveDeclaredColumn), targetSchemaName)));
             viewRecreates++;
             continue;
         }
@@ -745,6 +745,28 @@ function applyIndexDefaults(indexStmt, targetSchemaName) {
     }
     return result;
 }
+/**
+ * Applies schema name to a view statement.
+ * Mirror of applyTableDefaults/applyIndexDefaults: qualify the view with the target
+ * schema so `apply schema App { view X }` creates App.X (not main.X). Without this,
+ * unqualified views default to 'main' (create-view.js), so a CHECK constraint on an
+ * App table resolving a sibling view against its own schema path ('App') fails with
+ * "Table 'X' not found in schema path: App".
+ *
+ * VoteTorrent patch: forward-ported from the 3.3.0 patch (Piece 1) to 4.2.1 → 4.3.1 → 4.4.1.
+ */
+function applyViewDefaults(viewStmt, targetSchemaName) {
+    if (targetSchemaName && targetSchemaName !== 'main' && !viewStmt.view.schema) {
+        return {
+            ...viewStmt,
+            view: {
+                ...viewStmt.view,
+                schema: targetSchemaName
+            }
+        };
+    }
+    return viewStmt;
+}
 /**
  * Reads an index column's EXPLICIT per-column collation as-written, covering both
  * indexed-column forms: the plain `col.collation` and the parser's collate-folded
