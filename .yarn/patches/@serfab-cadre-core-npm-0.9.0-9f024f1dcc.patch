diff --git a/dist/cadre-node.js b/dist/cadre-node.js
index 5dbf06119d6be370457f9edbc9ac4db68cb77373..06b461d015047302c42197f70897d4ebea46c730 100644
--- a/dist/cadre-node.js
+++ b/dist/cadre-node.js
@@ -1923,7 +1923,13 @@ export class CadreNode {
             strandRow: strand,
             sAppConfig,
             storage: this.config.storage,
-            network: this.config.network,
+            // P2P-11 (41-11): additive per-strand relay-path override. When a caller supplies
+            // `strandNetwork`, the strand libp2p node reserves through a DISTINCT relay target
+            // from the control node's — so the circuit-relay-v2 server (keyed purely by the
+            // shared PeerId, server/index.js:230-236 connections[0]) holds only ONE connection
+            // per PeerId and cannot misroute strand streams to the control connection. Callers
+            // that do not set `strandNetwork` see `this.config.network` verbatim — zero change.
+            network: this.config.strandNetwork ?? this.config.network,
             profile: this.config.profile,
             defaultLatencyHint: this.config.hibernation?.defaultLatencyHint ?? 'interactive',
             privateKey: this.identityKey,
diff --git a/dist/types.d.ts b/dist/types.d.ts
index f24712202c3796225682363827e98d17a4577aea..e2c73de9170660d9f55081eeb292d0dd705dcd1c 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -237,6 +237,13 @@ export interface CadreNodeConfig {
     storage?: StorageConfig;
     /** Network configuration */
     network?: NetworkConfig;
+    /**
+     * P2P-11 (41-11): optional per-strand network override. When set, strand libp2p nodes use
+     * this config instead of {@link network} (control node still uses {@link network}). Lets the
+     * strand node reserve through a DISTINCT relay target than the control node, avoiding the
+     * shared-PeerId circuit-relay-v2 hop-connect collision. Unset ⇒ strands reuse {@link network}.
+     */
+    strandNetwork?: NetworkConfig;
     /** Hibernation configuration */
     hibernation?: HibernationConfig;
     /** Polling interval for strand watcher in ms (default: 5000) */
