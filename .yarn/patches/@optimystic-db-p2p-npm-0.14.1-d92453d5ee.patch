diff --git a/dist/src/libp2p-node-base.js b/dist/src/libp2p-node-base.js
index f19e490a4b55c89883135add9960ba7366f90f3b..2b6e8d4b02f348548eb20f866f91f4440e2125f3 100644
--- a/dist/src/libp2p-node-base.js
+++ b/dist/src/libp2p-node-base.js
@@ -1,7 +1,7 @@
 import { createLibp2p } from 'libp2p';
 import { noise } from '@chainsafe/libp2p-noise';
 import { yamux } from '@chainsafe/libp2p-yamux';
-import { identify } from '@libp2p/identify';
+import { identify, identifyPush } from '@libp2p/identify';
 import { ping } from '@libp2p/ping';
 import { dcutr } from '@libp2p/dcutr';
 import { autoNAT } from '@libp2p/autonat';
@@ -161,8 +161,34 @@ export async function createLibp2pNodeBase(options, defaults) {
         connectionEncrypters: [noise()],
         streamMuxers: [yamux()],
         services: {
+            // VoteTorrent patch (41-06): @libp2p/identify's Identify/IdentifyPush constructors
+            // build their protocol string as `/${protocolPrefix}/id[/push]/<version>` (they
+            // ALWAYS prepend a leading slash — identify.js:16, identify-push.js:20; the
+            // documented default is the bare 'ipfs', utils.js:11 `protocolPrefix: 'ipfs'`).
+            // Passing an ALREADY-slash-prefixed value here (`/optimystic/${networkName}`)
+            // therefore yielded a malformed DOUBLE-SLASH protocol string
+            // (`//optimystic/strand-<id>/id/1.0.0`) — a genuine published-package string-
+            // construction footgun (device-observed 41-05 D-08 histogram, 6x). Strip the
+            // leading slash so both identify and identifyPush negotiate the intended
+            // single-slash `/optimystic/<networkName>/id[/push]/1.0.0`. Every OTHER service
+            // below (cluster/repo/sync/blockTransfer) builds its OWN protocol string directly
+            // via template literal (`${protocolPrefix}/repo/1.0.0`, no library-side prepend) —
+            // those are NOT affected and keep the leading-slash `protocolPrefix` value.
             identify: identify({
-                protocolPrefix: `/optimystic/${options.networkName}`
+                protocolPrefix: `optimystic/${options.networkName}`
+            }),
+            // VoteTorrent patch (41-04): identify() alone only exchanges address info at
+            // connection-open time. A relay-only (NAT'd) peer's circuit-relay-v2 reservation
+            // typically completes AFTER its first connection to a sibling (the connection is
+            // what requests the reservation in the first place), so without identifyPush the
+            // sibling's peerStore never learns the peer's relay-routed dial-back address --
+            // a later re-dial by peer id (e.g. after the original connection drops) throws
+            // "no valid addresses for peer" even though the peer IS reachable through the
+            // relay. identifyPush listens for the local 'self:peer:update' event (fired by
+            // AddressManager once the reservation's 'listening' event lands) and proactively
+            // pushes the updated address list to every already-connected peer.
+            identifyPush: identifyPush({
+                protocolPrefix: `optimystic/${options.networkName}`
             }),
             ping: ping(),
             // DCUtR (hole-punch) upgrades relayed node↔node connections to direct
