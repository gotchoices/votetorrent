---
phase: 12.2-extend-test-fixtures-for-e2e-seeding
plan: 03
type: execute
wave: 3
depends_on:
  - 12.2-02
files_modified:
  - packages/vote-engine/test/elections.spec.ts
  - packages/vote-engine/test/election.spec.ts
  - packages/vote-engine/test/network.spec.ts
  - packages/vote-engine/test/authority.spec.ts
  - packages/vote-engine/test/user.spec.ts
  - packages/vote-engine/test/tasks-builders.spec.ts
  - packages/vote-engine/test/signing.spec.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-03
  - TEST-05
  - BTEST-02
must_haves:
  truths:
    - "Election creation tests (createElection, getDetails, revisions, proposeBallot, proposeQuestion, keyholder) are unskipped and passing"
    - "Election builder equivalence smokes (elections + election spec files) are unskipped and passing"
    - "Network createAuthority tests are unskipped and passing using seedAuthorityInvite"
    - "User builder create tests use makeDistinctTestUser and are unskipped"
    - "Authority builder equivalence smokes (proposeAdmin, saveInviteWithSigning) are unskipped and passing"
    - "Tests blocked by quereus upstream issues (ProposedNetwork PK, zero-column PK, OfficerSignature PK) remain skipped with D-09 annotations"
  artifacts:
    - path: "packages/vote-engine/test/elections.spec.ts"
      provides: "Unskipped election creation + builder tests"
      contains: "seedElectionSigning"
    - path: "packages/vote-engine/test/election.spec.ts"
      provides: "Unskipped election builder tests"
      contains: "addTestElection"
    - path: "packages/vote-engine/test/network.spec.ts"
      provides: "Unskipped createAuthority + builder tests (4 invite-blocked)"
      contains: "seedAuthorityInvite"
    - path: "packages/vote-engine/test/user.spec.ts"
      provides: "Unskipped UserCreateBuilder tests"
      contains: "makeDistinctTestUser"
    - path: "packages/vote-engine/test/authority.spec.ts"
      provides: "Unskipped authority builder equivalence smokes"
      contains: "seedAuthorityInvite"
  key_links:
    - from: "packages/vote-engine/test/elections.spec.ts"
      to: "packages/vote-engine/test/fixtures/test-context.ts"
      via: "import seedElectionSigning, addTestElection"
      pattern: "seedElectionSigning"
    - from: "packages/vote-engine/test/network.spec.ts"
      to: "packages/vote-engine/test/fixtures/test-context.ts"
      via: "import seedAuthorityInvite"
      pattern: "seedAuthorityInvite"
    - from: "packages/vote-engine/test/user.spec.ts"
      to: "packages/vote-engine/test/fixtures/test-context.ts"
      via: "import makeDistinctTestUser"
      pattern: "makeDistinctTestUser"
---

<objective>
Unskip ~35 tests across 7 spec files using the seeding helpers from Plan 02. Per D-08: aggressive unskip -- write bodies, run suite, re-skip only what is genuinely still blocked. Per D-09: tests blocked by quereus upstream issues (ProposedNetwork PK, zero-column PK, OfficerSignature PK) remain skipped.

Purpose: Close the pending test count from ~42 to ~7 (D-09 quereus issues only). This completes TEST-01 (authority), TEST-03 (network), TEST-05 (elections), and BTEST-02 (builder equivalence) coverage for Phase 12.2.

Output: 7 modified spec files with ~35 tests unskipped; test suite at ~558 passing / ~7 pending / 0 failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-extend-test-fixtures-for-e2e-seeding/12.2-RESEARCH.md
@.planning/phases/12.2-extend-test-fixtures-for-e2e-seeding/12.2-PATTERNS.md
@.planning/phases/12.2-extend-test-fixtures-for-e2e-seeding/12.2-01-SUMMARY.md
@.planning/phases/12.2-extend-test-fixtures-for-e2e-seeding/12.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unskip election pipeline tests in elections.spec.ts and election.spec.ts</name>
  <files>packages/vote-engine/test/elections.spec.ts, packages/vote-engine/test/election.spec.ts</files>
  <read_first>
    packages/vote-engine/test/elections.spec.ts
    packages/vote-engine/test/election.spec.ts
    packages/vote-engine/test/fixtures/test-context.ts
    packages/vote-engine/src/elections/elections-engine.ts
    packages/vote-engine/src/election/election-engine.ts
  </read_first>
  <action>
**elections.spec.ts (14 skipped tests):**

All 14 skipped tests are blocked by "DateValid CHECK" or "createElection blocked" -- both resolved by Plan 01 (date fix) + Plan 02 (seedElectionSigning + addTestElection rewrite).

For each `it.skip(...)` test:
1. Change `it.skip` to `it`
2. Import `addTestElection`, `seedElectionSigning`, `makeElectionInit`, `peekNextElectionTid` from `./fixtures/test-context.js` and `peekNextElectionTid` from the elections-engine if not already imported
3. In tests that call `addTestElection(auth)` -- the rewritten helper now returns a guaranteed non-null `electionEngine`. Remove any null checks / conditional skips on `electionEngine`.
4. In the `createElection` test (line ~238): use the `addTestElection` helper directly rather than building the pipeline manually -- the helper already seeds signing and calls createElection.
5. In tests that need election context (getDetails, revisions, proposeBallot, proposeQuestion, proposeOption, keyholder): use `addTestElection(auth)` to get `TestElectionContext`, then call the method under test on `electionsEngine` or `electionEngine`.

For task-related tests in elections.spec.ts (completeKeyRelease line ~631, completeSignature line ~729, setOnboardingTaskCompleted line ~785): these need Task rows seeded. For the onboarding task test: seed Onboarding + Task + OnboardingTaskExtension rows directly with minimal raw SQL inserts using `IsMutationValid = true` bypass as noted in Research Open Questions Q3 (the bypass is minimal -- Onboarding has no behavioral constraints). For the key release and signature task tests: attempt to unskip by seeding via the election creation pipeline; if a genuine blocker remains (e.g., the engine does not expose a task-seeding path), re-skip with an updated annotation explaining the specific residual blocker.

For the three builder equivalence tests at the bottom (lines ~847, ~872, ~905): unskip and use `addTestElection` for the seeding. The builder `fromPayload(init).commit()` path needs the same signing pipeline -- ensure the signing nonce is seeded before calling `commit()`. If the builder's `commit()` calls `createElection()` internally and needs its own signing nonce, seed a fresh signing nonce for the builder path using `peekNextElectionTid()` and `seedElectionSigning()`.

**election.spec.ts (9 skipped tests):**

All 9 are blocked by "ElectionIdValid CHECK (no Election row)." Use `addTestElection(auth)` to seed the Election row, then unskip.

For each of the 9 tests:
1. Change `it.skip` to `it`
2. Ensure the test's `beforeEach` or test body calls `addTestElection` to get `TestElectionContext`
3. Use `electionEngine` from the context to call the method under test (proposeBallot, proposeRevision, inviteKeyholder)
4. For builder equivalence smokes: seed the election, then run the builder path

If `proposeQuestion` or `proposeOption` tests remain blocked by NOT NULL constraint on `DependsOn` / `Details` (engine doesn't bind these fields), re-skip with updated annotation.
  </action>
  <verify>
    <automated>cd packages/vote-engine && yarn test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - elections.spec.ts `it.skip` count drops from 14 to at most 3 (task tests may remain skipped if no seeding path exists)
    - election.spec.ts `it.skip` count drops from 9 to at most 2 (proposeQuestion/proposeOption may remain if engine binding gaps exist)
    - elections.spec.ts imports `addTestElection` or `seedElectionSigning` from `./fixtures/test-context.js`
    - `yarn workspace @votetorrent/vote-engine test` reports 0 failing
    - Any re-skipped tests have updated annotations explaining the specific residual blocker (not generic "DateValid" or "quereus#23")
  </acceptance_criteria>
  <done>Election pipeline tests unskipped and passing; election builder tests unskipped and passing; remaining skips have specific residual blocker annotations.</done>
</task>

<task type="auto">
  <name>Task 2: Unskip network, authority, user, tasks, and signing tests</name>
  <files>packages/vote-engine/test/network.spec.ts, packages/vote-engine/test/authority.spec.ts, packages/vote-engine/test/user.spec.ts, packages/vote-engine/test/tasks-builders.spec.ts, packages/vote-engine/test/signing.spec.ts</files>
  <read_first>
    packages/vote-engine/test/network.spec.ts
    packages/vote-engine/test/authority.spec.ts
    packages/vote-engine/test/user.spec.ts
    packages/vote-engine/test/tasks-builders.spec.ts
    packages/vote-engine/test/signing.spec.ts
    packages/vote-engine/test/fixtures/test-context.ts
    packages/vote-engine/src/network/network-engine.ts
    packages/vote-engine/src/authority/authority-engine.ts
  </read_first>
  <action>
**network.spec.ts (11 skipped tests):**

- 4 createAuthority tests (lines ~410, ~438, ~551, ~585): These need the invite flow. Import `seedAuthorityInvite` from `./fixtures/test-context.js`. In each test: call `seedAuthorityInvite(auth)` to seed InviteSlot + InviteResult, then call `networkEngine.createAuthority(authorityInit, adminInit)`. The `createAuthority` method's `Authority.InsertValid` CHECK should now have the required InviteSlot/InviteResult rows. However, `createAuthority()` currently hardcodes `SigningNonce = null, InviteSlotCid = null, InviteSignature = null` in its context bindings. The invite flow may require these to be populated. If `createAuthority` still fails because the context bindings don't carry the invite data, update the test to see if additional context wiring is needed and re-skip with a specific annotation if the engine needs modification to pass invite context.
- 3 proposeRevision builder tests (lines ~2847, ~2874, ~2920): These are blocked by "ProposedNetwork single-row PK" -- this is a D-09 quereus upstream issue. Keep these skipped with existing annotation.
- 1 proposeRevision test (line ~743): Also blocked by ProposedNetwork PK. Keep skipped.
- 3 createAuthority builder tests (lines ~2468, ~2494, ~2535): These need the invite flow. Apply the same `seedAuthorityInvite` pattern.

**authority.spec.ts (5 skipped tests):**

- 2 builder equivalence smokes (proposeAdmin line ~2576, saveInviteWithSigning line ~2687): These need AdminSignature rows. For proposeAdmin: seed a signing session and call `proposeAdmin` via the real engine. Use `seedElectionSigning` or `startSigningSession` to create AdminSigning + AdminSignature with the correct digest for proposeAdmin. The `proposeAdmin` test needs `AdminDigestArgs`-based signing (PATH A), not election signing. Import `SigningEngine` and call `startSigningSession` directly with the proposeAdmin digest args. For saveInviteWithSigning: call `seedAuthorityInvite(auth)` which internally calls `saveInviteWithSigning`.
- 3 remaining skipped tests: examine skip reasons and unskip if the blocking condition is resolved.

**user.spec.ts (4 skipped tests -- 3 in scope):**

- 3 UserCreateBuilder tests (lines ~797, ~826, ~871): Blocked by User PK collision. Import `makeDistinctTestUser` from `./fixtures/test-context.js`. In each test, replace the user payload with `makeDistinctTestUser()` so the UserEngine.create() call uses a fresh UUID that does not collide with the `'user-1'` already seeded by createTestNetwork(). Unskip all three.
- 1 test at line 94 (helper comment, not a skip): no action.

**tasks-builders.spec.ts (2 skipped tests):**

- completeSignature builder smoke (line ~302): Blocked by missing Task row. Attempt to seed: use `addTestElection` to create election rows, then check if the engine provides a way to seed Task + SignatureTaskExtension rows. If no engine method exists to create these rows, seed via raw SQL INSERT with `IsMutationValid = true` bypass (minimal -- Task has no complex behavioral constraints beyond InsertOnly). If the test still fails, re-skip with updated annotation.

**signing.spec.ts (1 skipped test):**

- sign builder equivalence smoke (line ~574): Blocked by OfficerSignature PK collision (`startSigningSession` calls `sign()` internally, then the test tries `sign()` again with same nonce+userId). Per Research Pitfall 4: this requires either a second signer or remains D-09. Attempt to fix by creating a two-officer authority (threshold=2) so `startSigningSession` inserts one OfficerSignature but does not reach threshold, then the test's `sign()` call uses a second user ID. If this is too complex or requires significant engine changes, re-skip with D-09 annotation noting "OfficerSignature PK structural limit."

Per D-08: aggressive unskip strategy. Run the suite after each file change. Any test that still fails gets re-skipped with a precise annotation (not generic "quereus#23").
  </action>
  <verify>
    <automated>cd packages/vote-engine && yarn test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - network.spec.ts `it.skip` count drops from 11 to at most 4 (the 4 ProposedNetwork PK D-09 tests)
    - authority.spec.ts `it.skip` count drops from 5 to at most 0
    - user.spec.ts `it.skip` count drops from 4 to at most 1 (the non-builder skip if present)
    - tasks-builders.spec.ts `it.skip` count drops from 2 to at most 1
    - signing.spec.ts `it.skip` count drops from 1 to 0 or stays at 1 with updated D-09 annotation
    - `yarn workspace @votetorrent/vote-engine test` reports 0 failing
    - Total pending across all spec files is <= 10
    - Total passing is >= 550 (up from 523 baseline)
    - Any re-skipped test has updated annotation explaining specific residual blocker
  </acceptance_criteria>
  <done>All addressable tests across network, authority, user, tasks, and signing specs are unskipped and passing; remaining skips are exclusively D-09 quereus upstream issues or structural PK limits with precise annotations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test spec -> test-context helpers | Test files import and call composable helpers; fixture data only |
| test-context helpers -> engine | Helpers call real engine methods with test data; no production boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.2-05 | Tampering | Raw SQL seeds in task tests | accept | Minimal bypass (`IsMutationValid = true`) for Onboarding/Task rows only; these tables have no behavioral constraints beyond InsertOnly. Documented inline per Research Open Questions Q3. |
| T-12.2-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
Full suite: `yarn workspace @votetorrent/vote-engine test` reports >= 550 passing, 0 failing, <= 10 pending.
Skip audit: `grep -c 'it\.skip' packages/vote-engine/test/*.spec.ts` total across all files <= 10.
D-09 check: every remaining `it.skip` annotation contains a specific blocker description (not generic "quereus#23" or "DateValid").
Coverage: `grep -c 'seedElectionSigning\|seedAuthorityInvite\|makeDistinctTestUser' packages/vote-engine/test/*.spec.ts` shows imports across multiple files.
</verification>

<success_criteria>
1. Total passing tests >= 550 (up from 523 baseline).
2. Total pending tests <= 10 (down from 42).
3. 0 failing tests.
4. Every remaining it.skip has a specific, non-generic annotation.
5. Elections, election, network, authority, user, tasks-builders, and signing spec files all updated.
</success_criteria>

<output>
Create `.planning/phases/12.2-extend-test-fixtures-for-e2e-seeding/12.2-03-SUMMARY.md` when done
</output>
