# Phase 12.2: Extend Test Fixtures for E2E Seeding - Validation

**Created:** 2026-05-27
**Source:** RESEARCH.md Validation Architecture section

---

## Test Framework

| Property | Value |
|----------|-------|
| Framework | Mocha `^11.5.0` + Chai `^5.2.0` |
| Config file | None (CLI args in `package.json` `"test"` script) |
| Quick run command | `yarn workspace @votetorrent/vote-engine test` |
| Full suite command | `yarn workspace @votetorrent/vote-engine test` (same -- no split) |

## Phase Requirements to Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| TEST-01 | authority.spec.ts proposeAdmin + saveInviteWithSigning builder smokes | integration | `yarn workspace @votetorrent/vote-engine test` | Yes (tests are skipped, to be unskipped in Plan 03) |
| TEST-03 | network.spec.ts createAuthority + builder smokes | integration | `yarn workspace @votetorrent/vote-engine test` | Yes (tests are skipped, to be unskipped in Plan 03) |
| TEST-05 | elections.spec.ts createElection, getElectionDetails, addBallot | integration | `yarn workspace @votetorrent/vote-engine test` | Yes (tests are skipped, to be unskipped in Plan 03) |
| BTEST-02 | builder equivalence smokes across 6 spec files | integration | `yarn workspace @votetorrent/vote-engine test` | Yes (tests are skipped, to be unskipped in Plan 03) |

## Sampling Rate

- **Per task commit:** `yarn workspace @votetorrent/vote-engine test` -- full suite (~7s runtime)
- **Per wave merge:** `yarn workspace @votetorrent/vote-engine test`
- **Phase gate:** Full suite green (0 failing, <=7 pending for D-09 issues) before `/gsd-verify-work`

## Wave 0 Gaps

- [x] Fix `NetworksEngine.create()` batch execution order (Admin before Officer in separate exec calls) -- Plan 01 Task 1
- [x] Fix `NetworkEngine.createAuthority()` batch execution + invite context params -- Plan 01 Task 1
- [x] Fix `ElectionsEngine.createElection()` date format and signing nonce -- Plan 01 Task 2

## Phase Gate Criteria

| Metric | Threshold | How to Verify |
|--------|-----------|---------------|
| Passing tests | >= 550 | `yarn workspace @votetorrent/vote-engine test` |
| Failing tests | 0 | `yarn workspace @votetorrent/vote-engine test` |
| Pending tests | <= 10 | `yarn workspace @votetorrent/vote-engine test` |
| Skip annotations | All specific, non-generic | `grep 'it\.skip' packages/vote-engine/test/*.spec.ts` |
