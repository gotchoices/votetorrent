---
phase: 12.3-close-non-quereus-pending-tests
plan: 04
type: execute
wave: 3
depends_on:
  - 12.3-03
files_modified:
  - packages/vote-engine/test/authority.spec.ts
  - packages/vote-engine/test/network.spec.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-03
must_haves:
  truths:
    - "authority.spec.ts:1786 queries CurrentAdmin.EffectiveAt and uses ctx.user?.id and ctx.user.activeKeys[0]?.key — no hardcoded 'user-1' or Date.now()"
    - "network.spec.ts:928 and :1020 query CurrentAdmin.EffectiveAt before seeding AdminSigning — no Date.now() bindings"
    - "All three tests are unskipped (it.skip → it) and pass; SignatureValid / digest-mismatch assertions are preserved"
  artifacts:
    - path: "packages/vote-engine/test/authority.spec.ts"
      provides: "Rewritten OfficerSignature-rejection test using ctx.user and CurrentAdmin lookup"
      contains: "CurrentAdmin"
    - path: "packages/vote-engine/test/network.spec.ts"
      provides: "Two rewritten AdminSigning tests using CurrentAdmin.EffectiveAt"
      contains: "CurrentAdmin"
  key_links:
    - from: "packages/vote-engine/test/authority.spec.ts"
      to: "packages/vote-engine/test/fixtures/test-context.ts"
      via: "ctx.user identity from createNetworkAndAuthority"
      pattern: "ctx\\.user"
---

<objective>
Group A — Rewrite 3 raw-SQL tests that currently hardcode `Date.now()` and `'user-1'` to use canonical-datetime values queried from `CurrentAdmin.EffectiveAt` and the actual seeded user identity.

Tests:
- `authority.spec.ts:1786` — should reject OfficerSignature when signature does not match digest
- `network.spec.ts:928` — should create AdminSigning session with scope rn and valid digest
- `network.spec.ts:1020` — should reject OfficerSignature when signature does not match AdminSigning digest

Per CONTEXT.md decisions: no new fixture helpers — inline the CurrentAdmin lookup.

Output: 2 modified spec files. 3 tests unskipped and passing. Suite at 0 failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.3-close-non-quereus-pending-tests/12.3-CONTEXT.md
@.planning/phases/12.2-extend-test-fixtures-for-e2e-seeding/12.2-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite authority.spec.ts:1786 OfficerSignature-rejection test</name>
  <files>packages/vote-engine/test/authority.spec.ts</files>
  <read_first>
    packages/vote-engine/test/authority.spec.ts
    packages/vote-engine/test/fixtures/test-context.ts
    packages/vote-core/schema/votetorrent.qsql
  </read_first>
  <action>
Locate the test at line ~1786 ("should reject OfficerSignature when signature does not match digest").

Changes:
1. Change `it.skip(` → `it(`.
2. Query `CurrentAdmin.EffectiveAt` for the seeded authority before inserting AdminSigning:
   `select EffectiveAt from CurrentAdmin where AuthorityId = :authorityId`
   Bind the result to `:effAt` in the AdminSigning INSERT. Do NOT pass `Date.now()`.
3. Replace hardcoded `'user-1'` with `ctx.user?.id` (the user actually seeded by `createNetworkAndAuthority`). If the test currently uses a raw signature object, set `SignerKey` to `(ctx.user?.activeKeys ?? [])[0]?.key`.
4. Preserve the test intent — the assertion that an OfficerSignature with a mismatched signature is rejected by SignatureValid. Only the seed setup changes.
5. Do not introduce new fixture helpers. Inline the CurrentAdmin SELECT.

Keep canonical-datetime contract: EffectiveAt comes from the DB as `YYYY-MM-DDTHH:MM:SS` and is bound as a string with no further conversion.
  </action>
  <verify>
    <automated>cd packages/vote-engine && yarn test test/authority.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <done>authority.spec.ts:1786 unskipped and passing; uses ctx.user and CurrentAdmin lookup; no hardcoded 'user-1' or Date.now().</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite network.spec.ts:928 and :1020 AdminSigning tests</name>
  <files>packages/vote-engine/test/network.spec.ts</files>
  <read_first>
    packages/vote-engine/test/network.spec.ts
    packages/vote-engine/test/fixtures/test-context.ts
  </read_first>
  <action>
Locate the two tests at ~line 928 ("should create AdminSigning session with scope rn and valid digest") and ~line 1020 ("should reject OfficerSignature when signature does not match AdminSigning digest").

For each test:
1. Change `it.skip(` → `it(`.
2. Before seeding AdminSigning, query `CurrentAdmin.EffectiveAt` and bind the canonical-string result to `:effAt`. Do not pass `Date.now()`.
3. Replace any hardcoded `'user-1'` with `ctx.user?.id` (use the actual seeded user from createNetworkAndAuthority).
4. Preserve the test intent — only the seed setup is rewritten.
5. Inline the lookup; no new fixture helper.
  </action>
  <verify>
    <automated>cd packages/vote-engine && yarn test test/network.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <done>network.spec.ts:928 and :1020 unskipped and passing; both use CurrentAdmin.EffectiveAt; no Date.now() bindings.</done>
</task>

</tasks>

<verification>
`grep -n 'Date.now()' packages/vote-engine/test/authority.spec.ts packages/vote-engine/test/network.spec.ts` returns no matches in the three rewritten tests.
`yarn workspace @votetorrent/vote-engine test 2>&1 | tail -5` reports 0 failing.
Test count rises by 3 from baseline.
</verification>

<success_criteria>
1. 3 tests (Group A) unskipped and passing.
2. Each test queries CurrentAdmin.EffectiveAt and uses ctx.user identity.
3. Suite at 0 failing.
</success_criteria>

<output>
Create `.planning/phases/12.3-close-non-quereus-pending-tests/12.3-04-SUMMARY.md` when done
</output>
