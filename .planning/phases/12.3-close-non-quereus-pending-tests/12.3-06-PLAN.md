---
phase: 12.3-close-non-quereus-pending-tests
plan: 06
type: execute
wave: 3
depends_on:
  - 12.3-02
  - 12.3-04
files_modified:
  - packages/vote-engine/test/network.spec.ts
autonomous: true
requirements:
  - TEST-03
  - BTEST-02
must_haves:
  truths:
    - "proposeRevision REAL ENGINE tests in network.spec.ts (lines 749, 2860, 2887, 2933) are unskipped"
    - "Second proposeRevision call against the same network succeeds — the ProposedNetwork composite PK from Plan 12.3-02 admits multiple rows per network"
    - "Existing single-revision tests still pass"
  artifacts:
    - path: "packages/vote-engine/test/network.spec.ts"
      provides: "Unskipped proposeRevision tests"
      contains: "proposeRevision"
  key_links:
    - from: "packages/vote-engine/test/network.spec.ts"
      to: "packages/vote-core/schema/votetorrent.qsql"
      via: "ProposedNetwork composite PK (Plan 12.3-02)"
      pattern: "proposeRevision"
---

<objective>
Group C unskip — Unskip the 3–4 pending `network.spec.ts` tests covering `proposeRevision`, now that Plan 12.3-02 has extended the `ProposedNetwork` PK to admit multiple revisions per network.

Tests (per CONTEXT.md):
- `network.spec.ts:749, 2860, 2887, 2933` — variants of "second proposeRevision" / "serializes imageRef as JSON or null".

Output: Modified `network.spec.ts`. 3–4 tests unskipped and passing. Suite at 0 failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.3-close-non-quereus-pending-tests/12.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unskip proposeRevision tests at lines 749, 2860, 2887, 2933</name>
  <files>packages/vote-engine/test/network.spec.ts</files>
  <read_first>
    packages/vote-engine/test/network.spec.ts
    packages/vote-engine/test/fixtures/test-context.ts
    packages/vote-core/schema/votetorrent.qsql
  </read_first>
  <action>
For each test at the four line numbers (pre-edit references):
1. Change `it.skip(` → `it(`.
2. Verify the body calls `networkEngine.proposeRevision(...)` and that the previous skip annotation cited the `ProposedNetwork` PK / UNIQUE-constraint blocker addressed by Plan 12.3-02.
3. If the test populates the discriminator column directly (e.g., a `Revision` field), confirm it matches the column name chosen in Plan 12.3-02. If the engine populates it implicitly, the test body should need no changes beyond unskip.
4. For tests covering imageRef JSON-or-null serialization, ensure both code paths exercise the new PK successfully.

If any test fails after unskip for a NEW residual blocker (not the PK issue), re-skip with a precise annotation describing the actual cause — do not regress to a generic annotation.
  </action>
  <verify>
    <automated>cd packages/vote-engine && yarn test test/network.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <done>3-4 proposeRevision tests unskipped and passing; ProposedNetwork PK change verified via test execution.</done>
</task>

</tasks>

<verification>
`grep -n 'ProposedNetwork.*PK\|single-row PK' packages/vote-engine/test/network.spec.ts` returns no remaining skip annotations citing this blocker.
`yarn workspace @votetorrent/vote-engine test 2>&1 | tail -5` reports 0 failing.
</verification>

<success_criteria>
1. 3-4 Group C tests unskipped.
2. All unskipped tests pass.
3. Suite at 0 failing.
</success_criteria>

<output>
Create `.planning/phases/12.3-close-non-quereus-pending-tests/12.3-06-SUMMARY.md` when done
</output>
