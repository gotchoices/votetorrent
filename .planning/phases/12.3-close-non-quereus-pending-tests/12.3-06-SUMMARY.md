---
phase: 12.3-close-non-quereus-pending-tests
plan: 06
subsystem: tests
tags: [tests, network, proposeRevision, unskip, group-c]
requires:
  - "Plan 12.3-02 (ProposedNetwork composite PK on (Name, Revision))"
provides:
  - "4 proposeRevision REAL ENGINE tests unskipped and passing"
affects:
  - packages/vote-engine/test/network.spec.ts
tech-stack:
  added: []
  patterns:
    - "Group C unskip — remove `.skip` annotations whose blocker has been resolved upstream"
key-files:
  created:
    - .planning/phases/12.3-close-non-quereus-pending-tests/12.3-06-SUMMARY.md
  modified:
    - packages/vote-engine/test/network.spec.ts
decisions:
  - "Strip the BLOCKED annotation entirely on unskip (rather than leaving a stale note) — the title now describes the test's actual assertion, matching the project convention used in 12.3-09."
metrics:
  duration_min: 4
  completed: 2026-05-28
---

# Phase 12.3 Plan 06: Unskip proposeRevision REAL ENGINE tests — Summary

One-liner: Unskipped 4 `network.spec.ts` tests that exercise a second `proposeRevision()` against the same network, now that Plan 12.3-02's `(Name, Revision)` composite PK on `ProposedNetwork` admits multiple rows.

## Goal

Group C unskip: remove the `.skip` from four tests in `packages/vote-engine/test/network.spec.ts` whose only blocker was the prior single-row PK on `ProposedNetwork`. Plan 12.3-02 fixed the schema; this plan retires the annotations and confirms the tests pass against the real engine.

## What Built

Modified `packages/vote-engine/test/network.spec.ts` — four `it.skip(...)` calls converted to `it(...)`:

| Pre-edit line | Test |
| ------------- | ---- |
| 754           | `proposeRevision > should serialize imageRef as JSON or null` |
| 2881          | `NetworkProposeRevisionBuilder REAL ENGINE: isValid===true => commit() does not throw BuilderValidationError` |
| 2908          | `NetworkProposeRevisionBuilder REAL ENGINE: double-commit guard throws BuilderAlreadyCommittedError` |
| 2954          | `NetworkProposeRevisionBuilder REAL ENGINE equivalence smoke: engine.proposeRevision(revision) vs builder.fromPayload(revision).commit()` |

The "second proposeRevision" pattern that previously tripped the UNIQUE constraint:
- Test 754 calls `engine.proposeRevision('WithImage')` followed by `engine.proposeRevision('NoImage')` against the same engine. Both inserts now succeed because the composite PK admits multiple Names (each with its own Revision sequence starting at 0).
- Tests 2881/2908/2954 each call `createTestNetwork()` once (the bootstrap already populates ProposedNetwork via `addInitialRevision`) and then call `proposeRevision` again via the builder — a second row that now lands under Revision = 1.
- Test 2908's double-commit guard fires synchronously in the builder before any engine call, so it never tripped the schema in the first place; unskip simply confirms the assertion path.

No test bodies were modified beyond the `it.skip` → `it` and annotation strip. No engine changes. No schema changes.

## Decisions Made

- **Strip the BLOCKED annotation entirely** rather than leaving "(unblocked by 12.3-02)" residue. The titles now read as plain behavioral descriptions, matching the convention used by Plan 12.3-09 when it unskipped the signing test.

## Plan vs. SUMMARY task accounting

- **Tasks planned:** 1 (Task 1: Unskip proposeRevision tests at lines 749/754, 2860/2881, 2887/2908, 2933/2954)
- **Tasks completed:** 1
- **Tasks deferred:** 0

The plan's referenced line numbers (749, 2860, 2887, 2933) were pre-12.3-04/05 references; the actual `.skip` lines on the current tree were 754, 2881, 2908, 2954 — line numbers had drifted by ~5–25 lines as expected after 12.3-04 and 12.3-05 edits in earlier blocks of the file. Test count (4) and test identities match.

## Verification

Plan-mandated verification:

- `grep -n 'ProposedNetwork.*PK\|single-row PK' packages/vote-engine/test/network.spec.ts` → no matches. **PASS.**
- `yarn workspace @votetorrent/vote-engine test 2>&1 | tail -5` reports **0 failing**. **PASS.**

Test counts before vs. after:

| Status   | Before | After | Delta |
| -------- | -----: | ----: | ----: |
| Passing  | 552    | 556   | +4    |
| Pending  | 21     | 17    | −4    |
| Failing  | 0      | 0     | 0     |

The four newly-passing tests all appear in the runner output:

```
proposeRevision
  ✔ should serialize imageRef as JSON or null
NetworkProposeRevisionBuilder
  ✔ REAL ENGINE: isValid===true => commit() does not throw BuilderValidationError
  ✔ REAL ENGINE: double-commit guard throws BuilderAlreadyCommittedError
  ✔ REAL ENGINE equivalence smoke: engine.proposeRevision(revision) vs builder.fromPayload(revision).commit()
```

## Deviations from Plan

None — single-task plan executed exactly as specified. The only minor adjustment was tracking the post-12.3-04/05 line numbers (documented above) rather than the pre-edit references in the plan.

### Architectural Changes

None.

### Authentication Gates

None.

## Known Stubs

None.

## Threat Flags

None — pure test unskip, no production surface changes.

## TDD Gate Compliance

Plan type is `execute`, not `tdd`. No RED/GREEN/REFACTOR gate sequence required.

## Deferred Issues

None.

## Commits

| Hash      | Type | Description                                               |
| --------- | ---- | --------------------------------------------------------- |
| `f904c3d` | test | unskip 4 proposeRevision REAL ENGINE tests                |

## Self-Check: PASSED

- File `packages/vote-engine/test/network.spec.ts` exists; `grep -c 'ProposedNetwork single-row PK\|second proposeRevision trips' packages/vote-engine/test/network.spec.ts` returns 0.
- Commit `f904c3d` exists on `worktree-agent-a01284093ac7f7f4d`.
- Verification: `yarn workspace @votetorrent/vote-engine test` reports **556 passing / 17 pending / 0 failing**.
