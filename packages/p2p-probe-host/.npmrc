# Run `npm install` (not yarn) from within this directory to install isolated deps.
# The --no-workspaces flag is required because npm would otherwise traverse to the
# Yarn workspace root which contains workspace: protocol entries that npm cannot resolve.
workspaces=false
